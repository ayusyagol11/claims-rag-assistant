# Core RAG framework — use llama-index-core to avoid the meta-package pulling in
# llama-index-llms-anthropic, which caps anthropic<0.29.0 and conflicts with 0.40.0.
# We call Anthropic directly (src/generate.py), so the LlamaIndex LLM integration is not needed.
llama-index-core>=0.11.0,<0.12.0
llama-index-embeddings-huggingface==0.3.1
llama-index-vector-stores-chroma==0.2.0

# Vector DB
chromadb==0.5.5

# LLM
anthropic==0.40.0

# PDF parsing
pypdf==4.3.1

# UI
streamlit==1.38.0

# Environment management
python-dotenv==1.0.1

# Evaluation
pandas==2.2.2
matplotlib==3.9.2