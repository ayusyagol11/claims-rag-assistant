# PyTorch CPU-only wheels — avoids 45-minute source compilation on Python versions
# that have no PyPI wheel (e.g. Python 3.14). Pre-built CPU wheels are available
# here for all Python versions torch supports.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core RAG framework — use llama-index-core to avoid the meta-package pulling in
# llama-index-llms-anthropic. We call Anthropic directly (src/generate.py), so the
# LlamaIndex LLM integration is not needed.
llama-index-core>=0.11.0,<0.12.0
llama-index-embeddings-huggingface==0.3.1
llama-index-vector-stores-chroma==0.2.0

# Vector DB
chromadb==0.5.5

# LLM
anthropic==0.28.1

# PDF parsing
pypdf==4.3.1

# UI
streamlit==1.38.0

# Environment management
python-dotenv==1.0.1

# Evaluation
pandas==2.2.2
matplotlib==3.9.2